<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="segment"
            type="com.lenta.bp7.data.model.Segment"/>

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="@dimen/list_row_height"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <LinearLayout
                style="@style/LinearCellStyle"
                android:layout_weight="@integer/size_column_1">

                <TextView
                    android:id="@+id/tv_item_number"
                    style="@style/CellTextStyle"
                    android:text="@{String.valueOf(segment.id)}"
                    tools:text="1"/>

            </LinearLayout>

            <View
                style="@style/RowVerticalDivider"/>

            <LinearLayout
                style="@style/LinearCellStyle"
                android:layout_weight="@integer/size_column_2">

                <TextView
                    android:id="@+id/tv_segment_number_date"
                    style="@style/CellTextStyle"
                    android:maxLines="2"
                    android:text="@{@string/segment_number_store_date(segment.number, segment.storeNumber, segment.formattedDate)}"
                    tools:text="123-456 (ТК - 0005)\n09.07.2019"/>

            </LinearLayout>

            <View
                style="@style/RowVerticalDivider"/>

            <LinearLayout
                style="@style/LinearCellStyle"
                android:layout_weight="@integer/size_column_3">

                <ImageView
                    android:id="@+id/iv_segment_status"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    app:segmentStatusIcon="@{segment.status}"
                    tools:src="@drawable/ic_lock_white_24dp"/>

            </LinearLayout>

            <View
                style="@style/RowVerticalDivider"/>

            <LinearLayout
                style="@style/LinearCellStyle"
                android:layout_weight="@integer/size_column_4">

                <TextView
                    android:id="@+id/tv_shelves_count"
                    style="@style/CellTextStyle"
                    android:text="@{String.valueOf(segment.numberOfShelves)}"
                    tools:text="5"/>

            </LinearLayout>

        </LinearLayout>

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/color_divider"/>

    </LinearLayout>

</layout>
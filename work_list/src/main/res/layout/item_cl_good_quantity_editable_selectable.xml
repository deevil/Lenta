<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="good"
            type="com.lenta.bp14.features.check_list.goods_list.ItemGoodUi" />

        <variable
            name="selectedForDelete"
            type="Boolean" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_selectable"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="@dimen/common_row_height"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <LinearLayout
                style="@style/LinearCellStyle"
                android:layout_weight="@integer/three_three_1">

                <TextView
                    android:id="@+id/tv_item_number"
                    style="@style/CellTextStyle"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:background="@drawable/bg_border_circle_white_selectable_for_delete"
                    android:gravity="center"
                    android:text="@{good.position}"
                    app:selected="@{selectedForDelete}"
                    tools:text="1" />

            </LinearLayout>

            <View style="@style/VerticalDivider" />

            <LinearLayout
                style="@style/LinearCellStyle"
                android:layout_weight="@integer/three_three_2">

                <TextView
                    style="@style/CellTextStyle"
                    android:maxLines="2"
                    android:text="@{good.name}"
                    tools:text="000021 Масло" />

            </LinearLayout>

            <View style="@style/VerticalDivider" />

            <LinearLayout
                style="@style/LinearCellStyle"
                android:layout_weight="@integer/three_three_3">

                <com.lenta.shared.view.EditTextWithSuffix
                    style="@style/CellTextStyle"
                    android:inputType="numberDecimal"
                    app:maxLengthForScanProtect="@{10}"
                    android:text="@={good.quantity}"
                    app:digitsForUom="@{good.units}"
                    app:suffix="@{good.units.name}"
                    tools:text="10 кор" />

            </LinearLayout>

        </LinearLayout>

        <View style="@style/HorizontalDivider" />

    </LinearLayout>

</layout>
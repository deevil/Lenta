<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="vm"
            type="com.lenta.bp14.features.not_exposed.good_info.GoodInfoNeViewModel" />

    </data>

    <ScrollView
        android:id="@+id/scrollView"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">


            <!-- Количество -->
            <LinearLayout
                style="@style/LinearRowStyle">

                <LinearLayout
                    style="@style/LinearCellStyle"
                    android:layout_weight="1">

                    <TextView
                        style="@style/CellTextStyle"
                        android:text="@string/quantity" />

                </LinearLayout>

                <View
                    style="@style/VerticalDivider" />

                <LinearLayout
                    style="@style/LinearCellStyle"
                    android:layout_weight="2">

                    <com.lenta.shared.view.EditTextWithSuffix
                        style="@style/CellTextStyle"
                        android:enabled="@{vm.isInputNumberEnabled()}"
                        android:inputType="numberDecimal"
                        android:text="@={vm.quantityField}"
                        app:digitsForUom="@{vm.originalProcessedProductInfo.uom}"
                        app:suffix="@{vm.originalProcessedProductInfo.uom.name}"
                        tools:text="5 шт" />

                </LinearLayout>

            </LinearLayout>

            <View
                style="@style/HorizontalDivider" />


            <!-- Запас -->
            <LinearLayout
                style="@style/LinearRowStyle">

                <LinearLayout
                    style="@style/LinearCellStyle"
                    android:layout_weight="1">

                    <TextView
                        style="@style/CellTextStyle"
                        android:text="@string/market_storage" />

                </LinearLayout>

                <View
                    style="@style/VerticalDivider" />

                <LinearLayout
                    style="@style/LinearCellStyle"
                    android:layout_weight="2">

                    <TextView
                        android:id="@+id/tv_removed_good"
                        style="@style/CellTextStyle"
                        android:text="@{vm.marketStorage}"
                        tools:text="25 шт" />

                </LinearLayout>

            </LinearLayout>

            <View
                style="@style/HorizontalDivider" />


            <!-- Пустое место -->
            <LinearLayout
                style="@style/LinearRowStyle">

                <LinearLayout
                    style="@style/LinearCellStyle"
                    android:layout_weight="1">

                    <TextView
                        style="@style/CellTextStyle"
                        android:text="@string/empty_place" />

                </LinearLayout>

                <View
                    style="@style/VerticalDivider" />

                <LinearLayout
                    style="@style/LinearCellStyle"
                    android:layout_weight="2">

                    <TextView
                        android:id="@+id/tv_price_with_dk"
                        style="@style/CellTextStyle"
                        android:maxLines="2"
                        android:text='@{vm.isEmptyPlaceMarked == true ? @string/framed : @string/not_framed}'
                        app:invisible="@{vm.isEmptyPlaceMarked == null}"
                        tools:text="Оформлено" />

                </LinearLayout>

            </LinearLayout>

            <View
                style="@style/HorizontalDivider" />

        </LinearLayout>

    </ScrollView>

</layout>
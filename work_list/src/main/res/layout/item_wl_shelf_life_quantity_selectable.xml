<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="shelfLife"
            type="com.lenta.bp14.features.work_list.good_details.ItemShelfLifeUi" />

        <variable
            name="selectedForDelete"
            type="Boolean" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_selectable"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="@dimen/common_row_height"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <LinearLayout
                style="@style/LinearCellStyle"
                android:layout_weight="@integer/three_three_1">

                <TextView
                    android:id="@+id/tv_item_number"
                    style="@style/CellCircleNumberStyle"
                    android:text="@{shelfLife.position}"
                    app:selected="@{selectedForDelete}"
                    tools:text="1" />

            </LinearLayout>

            <View style="@style/VerticalDivider" />

            <LinearLayout
                style="@style/LinearCellStyle"
                android:layout_weight="@integer/three_three_2">

                <TextView
                    style="@style/CellTextStyle"
                    android:text="@{shelfLife.expirationDate}"
                    android:visibility="@{shelfLife.expirationDateVisibility}"
                    tools:text="СГ - 12.11.13" />

                <TextView
                    style="@style/CellTextStyle"
                    android:text="@{shelfLife.productionDate}"
                    android:visibility="@{shelfLife.productionDateVisibility}"
                    tools:text="ДП - 24.07.18" />

            </LinearLayout>

            <View style="@style/VerticalDivider" />

            <LinearLayout
                style="@style/LinearCellStyle"
                android:layout_weight="@integer/three_three_3">

                <TextView
                    style="@style/CellTextStyle"
                    android:layout_width="wrap_content"
                    android:text="@{shelfLife.quantity}"
                    tools:text="10 шт" />

            </LinearLayout>

        </LinearLayout>

        <View style="@style/HorizontalDivider" />

    </LinearLayout>

</layout>
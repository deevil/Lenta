<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="provider"
            type="com.lenta.bp14.features.work_list.good_info.ItemProviderUi" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_selectable"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="@dimen/common_row_height"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <LinearLayout
                style="@style/LinearCellStyle"
                android:layout_weight="@integer/number_provider_period_column_1">

                <TextView
                    android:id="@+id/tv_item_number"
                    style="@style/CellTextStyle"
                    android:layout_width="wrap_content"
                    android:text="@{provider.number}"
                    tools:text="1" />

            </LinearLayout>

            <View style="@style/VerticalDivider" />

            <LinearLayout
                style="@style/LinearCellStyle"
                android:layout_weight="@integer/number_provider_period_column_2">

                <TextView
                    android:id="@+id/tv_good_provider_number"
                    style="@style/CellTextStyle"
                    android:text="@{provider.code}"
                    tools:text="165832" />

                <TextView
                    android:id="@+id/tv_good_provider_name"
                    style="@style/CellTextStyle"
                    android:text="@{provider.name}"
                    tools:text="Поставщик 1" />

            </LinearLayout>

            <View style="@style/VerticalDivider" />

            <LinearLayout
                style="@style/LinearCellStyle"
                android:layout_weight="@integer/number_provider_period_column_3">

                <TextView
                    android:id="@+id/tv_period_kip"
                    style="@style/CellTextStyle"
                    android:text="@{provider.period}"
                    tools:text="12.05.19 - 01.12.19" />

            </LinearLayout>

        </LinearLayout>

        <View style="@style/HorizontalDivider" />

    </LinearLayout>

</layout>